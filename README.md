Project Approach:

I used TF-IDF Vectorization to convert textual data (JD and Resumes) into numerical vectors.

I calculated the Cosine Similarity to determine the relevance score between the JD and each resume.

The system sorts candidates based on the match percentage.

How to run: Open the file in Google Colab and run all cells.
